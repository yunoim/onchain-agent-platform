apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "oap.componentName" (dict "root" . "component" "litellm") }}
  labels:
    {{- include "oap.labels" . | nindent 4 }}
    app.kubernetes.io/component: litellm
data:
  config.yaml: |
    {{- .Values.litellm.config | nindent 4 }}

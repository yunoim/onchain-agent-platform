{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oap.serviceAccountName" . }}
  labels:
    {{- include "oap.labels" . | nindent 4 }}
# None of the services call the Kubernetes API; no token needed in the pods.
automountServiceAccountToken: false
{{- end }}
